const BASE_URL = "http://localhost:4444";

const API_PATHS = {
  USERS: "/users",
  POSTS: "/posts",
  COMMENTS: "/comments",
};

const ROUTE_PATHS = {
  HOME: "/",
  POST: "/post/:id",
};

export { BASE_URL, API_PATHS, ROUTE_PATHS };
